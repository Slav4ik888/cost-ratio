// import { createSelector } from '@reduxjs/toolkit';
// import { StateSchema } from 'app/providers/store';
// import { StateSchemaFactura } from '../slice/state-schema';


// export const selectModule = createSelector([(state: StateSchema) => state.factura || {} as StateSchemaFactura],
//   (state: StateSchemaFactura) => state);

// export const selectLoading = createSelector(selectModule, (state: StateSchemaFactura) => state.loading);
// export const selectErrors  = createSelector(selectModule, (state: StateSchemaFactura) => state.errors);

// export const selectFactura = createSelector(selectModule, (state: StateSchemaFactura) => state.factura);
